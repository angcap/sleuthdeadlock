logging:
    level:
        org:
            hibernate:
                SQL: DEBUG
server:
    port: ${port:8087}

spring:
    jpa:
        hibernate:
           naming:
              physical-strategy:
                 org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    jackson:
        serialization:
            write_dates_as_timestamps: false
